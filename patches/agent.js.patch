--- src/agent/agent.js
+++ src/agent/agent.js
@@ -463,8 +463,15 @@
             }
         });
         this.bot.on('death', () => {
-            this.actions.cancelResume();
-            this.actions.stop();
+            console.log('[DreamBot] Death detected — clearing actions and pathfinder.');
+            try { this.actions.cancelResume(); this.actions.stop(); } catch (e) {}
+            try {
+                if (this.bot.pathfinder) {
+                    this.bot.pathfinder.setGoal(null);
+                    this.bot.pathfinder.stop();
+                }
+            } catch (e) {}
+            try { this.bot.clearControlStates(); } catch (e) {}
         });
         this.bot.on('kicked', (reason) => {
             if (!this._disconnectHandled) {
+                const reasonStr = typeof reason === 'string' ? reason : JSON.stringify(reason);
+                console.log('[DreamBot] Kicked by server:', reasonStr);
+                // Mark so reconnect layer can apply long ban/throttle delay
+                try {
+                    process.stderr.write('[DreamBot][KICK] ' + reasonStr + '\n');
+                } catch (e) {}
