// DreamBot: stub — prismarine-viewer disabled on Railway
export function addBrowserViewer(bot, count_id) {
  // no-op
}
export function addViewer(bot, count_id) {
  // no-op
}
export default { addBrowserViewer, addViewer };
